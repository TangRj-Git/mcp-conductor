# Optional credentials used by configured upstream MCP servers.
# Put real values in .env or your shell environment. Do not commit secrets.

GITHUB_TOKEN=

# Add more project-level environment variable examples here when needed.
